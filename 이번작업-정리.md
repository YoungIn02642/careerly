# careerly 이번 작업 정리

> 이 문서는 **이번 세션에서 바꾸고 추가한 것**만 담았습니다.
> 전체 맥락은 `CAREERLY-작업정리.md` 를 봅니다.
> (아직 커밋하지 않았습니다.)

---

## 1. 바꾼 것 · 추가한 것 (요약)

### 회사 찾기
| 구분 | 내용 |
|------|------|
| 🐞 고침 | **회사명 자동완성이 한글일 때 안 되던 버그** — 글자마다 화면 전체를 다시 그려 입력창이 교체되면서 한글 조합(ㅅ→사→삼)이 깨졌습니다. 이제 드롭다운만 갈고 입력창은 그대로 둡니다. |
| 🐞 고침 | 리포트 화면의 사이드바 검색에도 드롭다운이 없어 Enter로만 열렸습니다 → 검색창 두 개를 **같은 핸들러로 통합**해 똑같이 동작합니다. |
| ✅ 추가 | **개요** — DART 공시(`empSttus`)를 붙여 직원 수 · 평균 근속연수 · 1인 평균급여 + 업종코드/설립일/대표자/상장 여부를 표시. 각 지표에 **근거로 담기** 버튼. |
| ✅ 추가 | **재무·실적** — DART 매출·증감률 + 3년 추이표. |
| ✅ 추가 | **경쟁사** — 업종코드 + 매출 0.3~3배 필터로 좁히고, 회사별 매출과 "우리의 몇 배"를 함께 표시. |
| 🐞 고침 | **뉴스에 야구·연예 기사 섞이던 문제** — KT위즈 같은 동명 구단 기사가 걸렸습니다. 스포츠 단어 종류 수를 세는 규칙 필터 + 같은 사건 중복 제거(겹침 0.25 이상 + 공통 고유명사)를 넣었습니다. AI는 안 씀(검색할 때마다 결과가 달라지면 근거로 못 씀). |
| ✅ 추가 | **채용공고** — 사람인 OpenAPI를 주 경로, 워크넷을 보조로 연결. |
| ⚙️ 성능 | 정적 파일에 `Cache-Control: no-cache` + ETag → 옛 JS/CSS가 캐시돼 안 바뀌어 보이던 문제 해결. 경쟁사 판정 DART 호출을 30분 메모리 캐시로 축소. |

### 자소서 코치
| 구분 | 내용 |
|------|------|
| 🎨 추가 | **색 입히기** — 입력 4항목·STAR 4칸·역량 목록에 분류색 배정(버튼 배경엔 안 씀). 사이드바 점 → 번호 배지 → 펼친 칸 왼쪽 띠까지 같은 색으로 연결. |
| ✅ 추가 | **내 자소서 보관함** — 회사별로 묶어 글자수·작성시각(1시간 전/2일 전) 표시, 펼쳐 읽기·복사·삭제·"이 회사로 이어쓰기". 예전 저장 형식도 그대로 읽음. |
| ✅ 추가 | **AI 초안** — 역량 하나에 대한 자소서 문단 초안 생성(아래 3장 참고). |

### 그 외 새 파일
- 백엔드: `dart.js`, `saramin-jobs.js`, `worknet-jobs.js`, `company-name.js`, `cover-guide.js`, `draft-coach.js`, `routes/companyAnalysis.js`
- 스크립트: `fetch-dart-corps.js`(상장사 3,981건 색인), `build-dart-industry.js`(업종코드 채우기)
- 테스트 추가: `dart`, `company-jobs`, `cover-guide`, `draft-coach`, `news` — 전체 통과

### 짚어둘 것
- **사람인 키**: `.env`의 `SARAMIN_ACCESS_KEY` 비어 있음 → 발급·승인 후 키만 넣으면 됩니다.
- **워크넷 키**: 개인회원 권한이라 공고를 못 받아옵니다(기업회원 전환 필요). 그래서 사람인을 주 경로로 둠.
- **DART 업종코드 색인**: 3,981개 회사 조회라 시간이 걸립니다. 끝나야 경쟁사 필터가 정확해집니다.

---

## 2. 사용 방법

### 회사 찾기 — 이렇게 쓰세요
1. 검색창에 **회사 이름을 입력**하면 자동완성 목록이 뜹니다 (예: `카카오` → 카카오/카카오뱅크/카카오페이/카카오게임즈). 원하는 회사를 고릅니다.
2. 리포트 화면에서 회사의 정보를 봅니다:
   - **개요** — 직원 수·평균 근속·평균급여 등 (DART 공시 기반)
   - **재무·실적** — 매출과 3년 추이
   - **경쟁사** — 매출 규모가 비슷한 같은 업종 회사
   - **뉴스** — 회사 관련 기사만 (야구·연예 걸러짐)
   - **채용공고** — 진행 중인 공고
3. 각 지표 옆의 **"근거로 담기"** 를 누르면 그 사실이 저장돼, 자소서 코치에서 근거 문장으로 씁니다.
4. 사이드바에서 다른 회사를 검색하면 리포트가 그 회사로 바뀝니다.

> 회사를 아직 안 고르면 리포트가 비어 있는 게 정상입니다. 먼저 검색해서 한 곳을 고르세요.

### 자소서 코치 — 이렇게 쓰세요
1. 지원할 **채용공고(직무기술서) 원문을 붙여넣습니다** (자격요건·우대사항·주요업무 부분 포함, 30자 이상).
2. **요구 역량 목록**이 나옵니다. 역량마다:
   - 기업이 이 역량으로 **무엇을 보는지**
   - 자소서에 녹이는 **문장 순서(frame)** 와 첫 문장 여는 방식
   - 이 직무 공고 중 몇 %가 요구하는지(시장 빈도)
3. (선택) 내 스펙의 **활동을 불러오면**, 역량마다 어울리는 활동이 배정됩니다.
4. 쓰고 싶은 역량에서 **AI 초안**을 누르면 그 역량에 대한 자소서 문단 초안이 나옵니다.
   - 모르는 사실은 **[대괄호]** 로 비어 있습니다 → 본인 사실로 채워야 합니다.
   - **빈칸이 남아 있으면 제출용으로 완성된 게 아닙니다.**
5. 화면의 검사기가 **상투어·AI 티 나는 표현**을 그 자리에서 표시합니다.
6. 완성한 글은 **내 자소서 보관함**에 회사별로 쌓입니다 — 다시 읽고, 복사하고, 이어쓸 수 있습니다.

> **이 기능은 자소서를 대신 써 주지 않습니다.** 무엇을 어떤 순서로 쓸지 안내하고,
> 첫 문단은 빈칸이 있는 틀로만 줍니다. 대필 문장은 유사도·AI 검출에 걸립니다.

---

## 3. 자소서 코치 AI는 어떻게 작동하나 (기술 설명)

핵심 원칙은 하나입니다 — **AI에게는 "분류"만 시키고, "문장"은 최대한 코드가 만든다.**
이유: 로컬 8B 모델은 산문 생성 품질이 낮고 검증 수단이 없으며, 매번 답이 달라지면 자소서 근거로 못 씁니다.

AI가 개입하는 지점은 두 곳이고, 둘 다 최소한입니다.

### ① 역량 추출 — `POST /api/jd/coach`
```
채용공고 원문
   │
   ▼
[1단계] 규칙 추출 (jd-competency.js)
   · ARCHETYPES 사전의 keyword로 공고에서 역량을 찾음
   · 대부분 여기서 끝남 (역량 4개 이상 잡히면 AI 안 부름)
   │
   ▼  (역량 4개 미만일 때만)
[2단계] AI 보강 (ai-provider → Groq 기본 / Ollama 대안)
   · AI가 하는 일: "역량 id 를 고르는 것"뿐 → 짧은 JSON
   · 근거가 된 공고 원문 문장(quote)만 그대로 옮김
   · 모르는 id는 서버가 버림 (지어낸 역량 방지)
   │
   ▼
[3단계] 가이드 조립 (전부 코드)
   · reads / frame / STAR / 시장빈도 문장은 사전에서 조립
   · AI는 문장을 한 줄도 쓰지 않음
```
→ **AI가 죽어도 규칙 결과만으로 화면이 완성됩니다.** (규칙도 못 잡을 때만 진짜 실패)

### ② 문단 초안 — `POST /api/jd/draft`
여기서만 AI가 실제 문장을 씁니다. 대신 프롬프트로 강하게 묶습니다 — **P.C.R.O 틀** (링커리어 자소서 AI 활용 프레임 차용):

| 요소 | 내용 |
|------|------|
| **Persona** | 15년차 인사담당자 + 현업 10년차 실무자로서 답함 |
| **Context** | 아는 것은 **회사·공고 근거 문장·요구 역량·사용자가 입력한 활동뿐**. 그 외는 모름. |
| **Restriction** | ①모르는 수치·기간·인원은 지어내지 말고 **[항목 이름] 대괄호로 비운다** ②상투어·AI 티 표현 금지(`cover-guide.js` 사전에서 읽음 — 검사기와 목록 일치) ③STAR 순서 ④역량 이름 직접 쓰지 않기 ⑤한국어로만 |
| **Output** | `{draft, blanks, review}` **JSON만** 출력 |

응답을 받은 뒤 **서버가 직접 검증**합니다:
- 첫 `{`부터 마지막 `}`까지 잘라 JSON 파싱 (실패 시 초안을 안 줌 — 깨진 문장을 넣게 두지 않음)
- **빈칸 개수를 서버가 문자열에서 직접 셈** (AI가 "빈칸 3개"라 말해도 실제 문장에서 센 값만 믿음)
- **상투어·AI 티 표현을 서버가 다시 스캔** (`findCliches`, `findAiTells`)

### 왜 이렇게 나눴나
- AI는 판단 여지가 클수록 결과가 흔들립니다 → 판단을 좁히고(분류만), 결과물은 코드가 결정론적으로 만듭니다. (CAS 점수 엔진의 rescore와 같은 원칙)
- 대필을 하면 표절·AI 검출에 그대로 걸립니다 → 완성 문장이 아니라 **빈칸 있는 틀**을 줍니다.
- 자소서 초안은 민감한 글이라 **검사·보관은 브라우저에서** 하고 서버에 저장하지 않습니다.

### 제공자(Provider)
- 기본 **Groq**, 대안 **Ollama(로컬 8B)** — `ai-provider.js` 한 겹으로 감쌈.
- 초안은 문단+빈칸안내+검토가 한 응답에 담기므로 `num_predict`를 넉넉히(1100) 줌 — 부족하면 JSON이 중간에 잘려 파싱이 통째로 실패.
